foreach (i in 1..3) {
    print(i);
    if (i == 1) {
        foreach (j in 1..2) {
            print(j);
        }
    }
    if (i == 2) {
        foreach (k in 1..3) {
            print(k);
        }
    }
}
