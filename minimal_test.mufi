// Minimal test to isolate the array iteration issue

print("Creating test array...");
var test_values = {0, 5, 10};
print("Array created successfully");

// Debug: Try to get length and manual indexing
print("Debug: Checking array properties...");
// Note: May need to check if length function exists
// print("Array length: " + test_values.length);

// Try manual indexing
print("Debug: Trying manual indexing...");
print("test_values[0] = " + test_values[0]);
print("test_values[1] = " + test_values[1]);
print("test_values[2] = " + test_values[2]);

print("Attempting to iterate over array...");
foreach (val in test_values) {
    print("DEBUG: Inside foreach loop");
    print("Value: " + val);
    print("DEBUG: End of loop iteration");
}

print("Test complete");
