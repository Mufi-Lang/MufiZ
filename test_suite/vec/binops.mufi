var a = linspace(1.0, 10.0, 10);
var b = linspace(2.0, 20.0, 10);
var c = linspace(3.0, 30.0, 10);

//assert(a*2.0, b); // test single mul
//assert(a + b, c); // test add vec

// we dont support binary operations on vectors yet
