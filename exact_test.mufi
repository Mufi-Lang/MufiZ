// Test the exact failing case from the original test

print("Testing the exact failing array case...");

// This is the exact line that was causing issues
var test_values = {0, 5, 10, 15, 20};
print("Array created: " + test_values);

// Test manual indexing first to confirm array works
print("Manual indexing test:");
print("test_values[0] = " + test_values[0]);
print("test_values[1] = " + test_values[1]);
print("test_values[2] = " + test_values[2]);
print("test_values[3] = " + test_values[3]);
print("test_values[4] = " + test_values[4]);

// Now try the exact foreach that was failing
print("Starting foreach iteration...");
foreach (test_val in test_values) {
    print("Processing value: " + test_val);
    print("This should work for each element");
}

print("Test completed successfully!");
