// Debug test file to isolate array iteration issue

print("=== Basic Array Creation Test ===");

// Test 1: Simple array creation
var arr1 = {1, 2, 3};
print("Array created: ");
print(arr1);

// Test 2: Array with more elements (like the failing case)
var arr2 = {0, 5, 10, 15, 20};
print("Larger array created: ");
print(arr2);

// Test 3: Try manual indexing first
print("=== Manual Indexing Test ===");
// This should work if the array is properly created
// print("arr2[0] = " + arr2[0]);
// print("arr2[1] = " + arr2[1]);

// Test 4: Simple foreach with small array
print("=== Simple Foreach Test ===");
var small_arr = {1, 2, 3};
print("Testing foreach with {1, 2, 3}:");
foreach (item in small_arr) {
    print("Item: " + item);
}

// Test 5: Foreach with stored array variable
print("=== Foreach with Variable Test ===");
print("Testing foreach with arr1 variable:");
foreach (item in arr1) {
    print("Item from arr1: " + item);
}

// Test 6: The problematic case (simplified)
print("=== Problematic Case Test ===");
var test_arr = {0, 5, 10};
print("Testing foreach with {0, 5, 10}:");
foreach (item in test_arr) {
    print("Item: " + item);
}

print("=== Test Complete ===");
