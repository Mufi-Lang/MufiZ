// Simple test for foreach with vectors
print "TEST SIMPLE FOREACH START";

// Define a vector with explicit values
var vec1 = {1.0, 2.0, 3.0};
print "Vector 1 defined with 3 elements";

// Test iterating over the vector
print "Iterating over vector 1:";
foreach (item in vec1) {
    print "Item:";
    print item;
}

// Define another vector
var vec2 = {10.0, 20.0};
print "Vector 2 defined with 2 elements";

// Test iterating over the second vector
print "Iterating over vector 2:";
foreach (x in vec2) {
    print "Value:";
    print x;
}

print "TEST SIMPLE FOREACH END";